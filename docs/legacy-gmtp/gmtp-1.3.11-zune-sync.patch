diff --color -u -r '--exclude=.deps' '--exclude=*.o' '--exclude=Makefile' '--exclude=gmtp' gmtp-1.3.11/src/about.c "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/about.c"
--- gmtp-1.3.11/src/about.c	2017-11-04 00:29:29.000000000 -0400
+++ "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/about.c"	2026-08-11 22:13:52.509895211 -0400
@@ -38,12 +38,12 @@
         NULL
     };
     dialog = gtk_about_dialog_new();
-    gtk_about_dialog_set_program_name(GTK_ABOUT_DIALOG(dialog), PACKAGE_NAME);
+    gtk_about_dialog_set_program_name(GTK_ABOUT_DIALOG(dialog), "Zune Sync");
     gtk_about_dialog_set_version(GTK_ABOUT_DIALOG(dialog), PACKAGE_VERSION);
     gtk_about_dialog_set_copyright(GTK_ABOUT_DIALOG(dialog),
-            "Copyright 2009-2016, Darran Kartaschew\nReleased under the BSD License");
+            "Based on gMTP\nCopyright 2009-2016, Darran Kartaschew\nReleased under the BSD License");
     gtk_about_dialog_set_comments(GTK_ABOUT_DIALOG(dialog),
-            _("A simple MTP Client for Solaris 10\nand other UNIX / UNIX-like systems\n"));
+            _("An MTP client for syncing music to the Microsoft Zune\n"));
     gtk_about_dialog_set_license(GTK_ABOUT_DIALOG(dialog),
             "gMTP License\n"
             "------------\n\n"
@@ -85,7 +85,7 @@
     gchar *version_string;
     gchar *gtk_version_string;
 
-    dialog = gtk_dialog_new_with_buttons(_("About gMTP"), GTK_WINDOW(windowMain),
+    dialog = gtk_dialog_new_with_buttons(_("About Zune Sync"), GTK_WINDOW(windowMain),
             (GTK_DIALOG_MODAL | GTK_DIALOG_DESTROY_WITH_PARENT),
             _("_Close"), GTK_RESPONSE_CLOSE,
             NULL);
diff --color -u -r '--exclude=.deps' '--exclude=*.o' '--exclude=Makefile' '--exclude=gmtp' gmtp-1.3.11/src/interface.c "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/interface.c"
--- gmtp-1.3.11/src/interface.c	2016-02-07 04:11:59.000000000 -0500
+++ "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/interface.c"	2026-08-11 22:11:38.280028141 -0400
@@ -40,8 +40,8 @@
 #include "formatdevice.h"
 #include "progress.h"
 
-void setupFileList();
-GtkTreeViewColumn *setupFolderList();
+void setupFileList(GtkTreeView *treeviewFiles);
+GtkTreeViewColumn *setupFolderList(GtkTreeView *treeviewFolders);
 void __fileRemove(GtkTreeRowReference *Row);
 void __fileDownload(GtkTreeRowReference *Row);
 void __folderRemove(GtkTreeRowReference *Row);
@@ -713,7 +713,7 @@
     gtk_tree_selection_set_mode(folderSelection, GTK_SELECTION_SINGLE);
 
     folderList = gtk_tree_store_new(NUM_FOL_COLUMNS, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_UINT, GDK_TYPE_PIXBUF);
-    folderColumn = setupFolderList(treeviewFolders);
+    folderColumn = setupFolderList(GTK_TREE_VIEW(treeviewFolders));
 
     folderListModel = gtk_tree_model_sort_new_with_model(GTK_TREE_MODEL(folderList));
     gtk_tree_sortable_set_sort_column_id(GTK_TREE_SORTABLE(folderListModel),
@@ -741,7 +741,7 @@
             G_TYPE_UINT, G_TYPE_BOOLEAN, G_TYPE_UINT64, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_UINT, G_TYPE_STRING,
             G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_UINT, GDK_TYPE_PIXBUF,
             G_TYPE_STRING);
-    setupFileList(treeviewFiles);
+    setupFileList(GTK_TREE_VIEW(treeviewFiles));
 
     fileListModel = gtk_tree_model_sort_new_with_model(GTK_TREE_MODEL(fileList));
     gtk_tree_sortable_set_sort_column_id(GTK_TREE_SORTABLE(fileListModel),
@@ -3134,9 +3134,9 @@
     gchar *winTitle;
 
     if (foldername == NULL) {
-        winTitle = g_strconcat(PACKAGE_NAME, NULL);
+        winTitle = g_strdup("Zune Sync");
     } else {
-        winTitle = g_strconcat(foldername, " - ", PACKAGE_NAME, NULL);
+        winTitle = g_strconcat(foldername, " - ", "Zune Sync", NULL);
     }
     gtk_window_set_title(GTK_WINDOW(windowMain), (winTitle));
     g_free(winTitle);
diff --color -u -r '--exclude=.deps' '--exclude=*.o' '--exclude=Makefile' '--exclude=gmtp' gmtp-1.3.11/src/main.c "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/main.c"
--- gmtp-1.3.11/src/main.c	2016-02-07 04:11:59.000000000 -0500
+++ "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/main.c"	2026-08-11 22:11:46.284965217 -0400
@@ -85,7 +85,7 @@
     gtk_set_locale();
 #endif
     g_set_prgname(PACKAGE_NAME);
-    g_set_application_name(PACKAGE_NAME);
+    g_set_application_name("Zune Sync");
 
     gtk_init(&argc, &argv);
     
diff --color -u -r '--exclude=.deps' '--exclude=*.o' '--exclude=Makefile' '--exclude=gmtp' gmtp-1.3.11/src/metatag_info.c "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/metatag_info.c"
--- gmtp-1.3.11/src/metatag_info.c	2016-02-07 04:11:59.000000000 -0500
+++ "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/metatag_info.c"	2026-08-11 22:31:04.915925359 -0400
@@ -281,11 +281,13 @@
 
 // ************************************************************************************************
 
-void get_id3_tags(gchar *filename, LIBMTP_track_t *trackinformation) {
+void get_id3_tags(gchar *filename, LIBMTP_track_t *trackinformation, gchar **albumartist) {
 
     MP3_Info mp3_information;
     struct id3_file * id3_file_id = id3_file_open(filename, ID3_FILE_MODE_READONLY);
 
+    if (albumartist != NULL) *albumartist = NULL;
+
     if (id3_file_id != NULL) {
         // We have a valid file, so lets get some data.
         struct id3_tag* id3_tag_id = id3_file_tag(id3_file_id);
@@ -296,6 +298,12 @@
         trackinformation->date = ID3_getFrameText(id3_tag_id, ID3_FRAME_YEAR);
         trackinformation->genre = ID3_getFrameText(id3_tag_id, ID3_FRAME_GENRE);
 
+        // TPE2 is the canonical ID3v2 Album Artist / Band field. Devices like the
+        // Zune group tracks by AlbumArtist, so surface it separately when present.
+        if (albumartist != NULL) {
+            *albumartist = ID3_getFrameText(id3_tag_id, "TPE2");
+        }
+
         gchar * tracknumber = ID3_getFrameText(id3_tag_id, ID3_FRAME_TRACK);
         if (tracknumber != 0) {
             trackinformation->tracknumber = atoi(tracknumber);
@@ -373,13 +381,15 @@
 
 // ************************************************************************************************
 
-void get_ogg_tags(gchar *filename, LIBMTP_track_t *trackinformation) {
+void get_ogg_tags(gchar *filename, LIBMTP_track_t *trackinformation, gchar **albumartist) {
     OggVorbis_File *mov_file = NULL;
     vorbis_info * mov_info = NULL;
     FILE *mfile;
     vorbis_comment *mov_file_comment = NULL;
     gchar * tracknumber = NULL;
 
+    if (albumartist != NULL) *albumartist = NULL;
+
     // Attempt to open the file, and init the OggVorbis_File struct for our file.
     // Yes I know about ov_fopen(), but Solaris 10 ships with vorbis 1.0.1 which
     // doesn't have this function.
@@ -404,6 +414,14 @@
     trackinformation->artist = OGG_getFieldText(mov_file_comment, "ARTIST");
     trackinformation->date = OGG_getFieldText(mov_file_comment, "DATE");
     trackinformation->genre = OGG_getFieldText(mov_file_comment, "GENRE");
+
+    if (albumartist != NULL) {
+        *albumartist = OGG_getFieldText(mov_file_comment, "ALBUMARTIST");
+        if (*albumartist == NULL)
+            *albumartist = OGG_getFieldText(mov_file_comment, "ALBUM_ARTIST");
+        if (*albumartist == NULL)
+            *albumartist = OGG_getFieldText(mov_file_comment, "ALBUM ARTIST");
+    }
     tracknumber = OGG_getFieldText(mov_file_comment, "TRACKNUMBER");
     if (tracknumber != NULL) {
         trackinformation->tracknumber = atoi(tracknumber);
@@ -440,11 +458,13 @@
 
 // ************************************************************************************************
 
-void get_flac_tags(gchar *filename, LIBMTP_track_t *trackinformation) {
+void get_flac_tags(gchar *filename, LIBMTP_track_t *trackinformation, gchar **albumartist) {
     FLAC__StreamMetadata *tags = NULL;
     FLAC__StreamMetadata streaminfo;
     gchar * tracknumber = 0;
 
+    if (albumartist != NULL) *albumartist = NULL;
+
     // Load in our tag information stream
     if (!FLAC__metadata_get_tags(filename, &tags))
         return;
@@ -458,6 +478,13 @@
     trackinformation->date = g_strdup(FLAC_getFieldText(tags, "DATE"));
     trackinformation->genre = g_strdup(FLAC_getFieldText(tags, "GENRE"));
 
+    if (albumartist != NULL) {
+        const gchar *aa = FLAC_getFieldText(tags, "ALBUMARTIST");
+        if (aa == NULL) aa = FLAC_getFieldText(tags, "ALBUM_ARTIST");
+        if (aa == NULL) aa = FLAC_getFieldText(tags, "ALBUM ARTIST");
+        if (aa != NULL) *albumartist = g_strdup(aa);
+    }
+
     tracknumber = FLAC_getFieldText(tags, "TRACKNUMBER");
     if (tracknumber != 0) {
         trackinformation->tracknumber = atoi(tracknumber);
@@ -487,7 +514,8 @@
 
 // ************************************************************************************************
 
-void get_asf_tags(gchar *filename, LIBMTP_track_t *trackinformation) {
+void get_asf_tags(gchar *filename, LIBMTP_track_t *trackinformation, gchar **albumartist) {
+    if (albumartist != NULL) *albumartist = NULL;
     FILE *ASF_File;
     GUID Header_GUID;
     GUID Stream_GUID;
@@ -622,16 +650,15 @@
                             if (g_ascii_strcasecmp(Descriptor_Name, "WM/AlbumTitle\0") == 0) {
                                 // We have the album Title;
                                 trackinformation->album = g_strdup(Descriptor_Value_Str);
-                            } else {
-                                if (g_ascii_strcasecmp(Descriptor_Name, "WM/Genre\0") == 0) {
-                                    // We have the album Genre;
-                                    trackinformation->genre = g_strdup(Descriptor_Value_Str);
-                                } else {
-                                    if (g_ascii_strcasecmp(Descriptor_Name, "WM/Year\0") == 0) {
-                                        // We have the album Year;
-                                        trackinformation->date = g_strdup(Descriptor_Value_Str);
-                                    }
-                                }
+                            } else if (g_ascii_strcasecmp(Descriptor_Name, "WM/Genre\0") == 0) {
+                                // We have the album Genre;
+                                trackinformation->genre = g_strdup(Descriptor_Value_Str);
+                            } else if (g_ascii_strcasecmp(Descriptor_Name, "WM/Year\0") == 0) {
+                                // We have the album Year;
+                                trackinformation->date = g_strdup(Descriptor_Value_Str);
+                            } else if (g_ascii_strcasecmp(Descriptor_Name, "WM/AlbumArtist\0") == 0) {
+                                if (albumartist != NULL && *albumartist == NULL)
+                                    *albumartist = g_strdup(Descriptor_Value_Str);
                             }
                             break;
                         case 2: // Boolean (DWORD)
diff --color -u -r '--exclude=.deps' '--exclude=*.o' '--exclude=Makefile' '--exclude=gmtp' gmtp-1.3.11/src/metatag_info.h "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/metatag_info.h"
--- gmtp-1.3.11/src/metatag_info.h	2016-02-07 04:11:59.000000000 -0500
+++ "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/metatag_info.h"	2026-08-11 22:30:48.822301356 -0400
@@ -93,26 +93,26 @@
      * @param filename filename of the MP3 file
      * @param trackinformation ID3 infomation returned via this struct.
      */
-    void get_id3_tags(gchar *filename, LIBMTP_track_t *trackinformation);
+    void get_id3_tags(gchar *filename, LIBMTP_track_t *trackinformation, gchar **albumartist);
     /**
      * Get our OGG track information
      * @param filename The OGG file to extract information
      * @param trackinformation Return the Trackinformation via this variable.
      */
-    void get_ogg_tags(gchar *filename, LIBMTP_track_t *trackinformation);
+    void get_ogg_tags(gchar *filename, LIBMTP_track_t *trackinformation, gchar **albumartist);
     /**
      * Get our FLAC track information
      * @param filename The FLAC file to extract information
      * @param trackinformation Return the Trackinformation via this variable.
      */
-    void get_flac_tags(gchar *filename, LIBMTP_track_t *trackinformation);
+    void get_flac_tags(gchar *filename, LIBMTP_track_t *trackinformation, gchar **albumartist);
     /**
      * Get our WMA track information. WMA and WMV are both contained in a ASF Container which the
      * container header has all the information. (No need to parse the audio streams themselves).
      * @param filename The WMA file to extract information
      * @param trackinformation Return the Trackinformation via this variable.
      */
-    void get_asf_tags(gchar *filename, LIBMTP_track_t *trackinformation);
+    void get_asf_tags(gchar *filename, LIBMTP_track_t *trackinformation, gchar **albumartist);
 
 #ifdef __cplusplus
 }
diff --color -u -r '--exclude=.deps' '--exclude=*.o' '--exclude=Makefile' '--exclude=gmtp' gmtp-1.3.11/src/mtp.c "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/mtp.c"
--- gmtp-1.3.11/src/mtp.c	2016-02-07 04:11:59.000000000 -0500
+++ "/home/james/Projects/Zune Sync/gmtp-1.3.11/src/mtp.c"	2026-08-11 23:21:13.290395639 -0400
@@ -600,6 +600,7 @@
     if (useTrack) {
         // We have an MP3/Ogg/FLAC/WMA file.
         LIBMTP_track_t *trackfile = LIBMTP_new_track_t();
+        gchar *albumArtistTag = NULL;
 
         trackfile->filesize = filesize;
         trackfile->filename = g_strdup(filename_stripped);
@@ -621,16 +622,16 @@
         switch (ret) {
             case LIBMTP_FILETYPE_MP3:
                 // We have an MP3 file, so use id3tag to read the metadata.
-                get_id3_tags(filename, trackfile);
+                get_id3_tags(filename, trackfile, &albumArtistTag);
                 break;
             case LIBMTP_FILETYPE_OGG:
-                get_ogg_tags(filename, trackfile);
+                get_ogg_tags(filename, trackfile, &albumArtistTag);
                 break;
             case LIBMTP_FILETYPE_FLAC:
-                get_flac_tags(filename, trackfile);
+                get_flac_tags(filename, trackfile, &albumArtistTag);
                 break;
             case LIBMTP_FILETYPE_WMA:
-                get_asf_tags(filename, trackfile);
+                get_asf_tags(filename, trackfile, &albumArtistTag);
                 break;
                 //break;
 
@@ -642,6 +643,11 @@
             trackfile->title = g_strdup(filename_stripped);
         if (trackfile->artist == NULL)
             trackfile->artist = g_strdup(_("<Unknown>"));
+        // Populate the dedicated AlbumArtist field (added by our patched libmtp).
+        // Sent as part of the LIBMTP_Update_Track_Metadata batch that also sets
+        // Artist/Album — the Zune accepts that atomic batch but rejects post-hoc
+        // single-property sets. Fall back to Artist when no dedicated tag exists.
+        trackfile->albumartist = g_strdup(albumArtistTag != NULL ? albumArtistTag : trackfile->artist);
         if (trackfile->date == NULL) {
             trackfile->date = g_strdup("");
         } else {
@@ -656,7 +662,10 @@
         // Update our album info, if we actually have an album.
         if (trackfile->album != NULL) {
             albuminfo->name = g_strdup(trackfile->album);
-            albuminfo->artist = g_strdup(trackfile->artist);
+            // Prefer a dedicated AlbumArtist tag (ID3 TPE2, Vorbis/FLAC ALBUMARTIST,
+            // WMA WM/AlbumArtist) over the track Artist so compilations and
+            // "feat." collaborations still group correctly on the device.
+            albuminfo->artist = g_strdup(albumArtistTag != NULL ? albumArtistTag : trackfile->artist);
             albuminfo->composer = NULL;
             albuminfo->genre = g_strdup(trackfile->genre);
         }
@@ -687,6 +696,10 @@
             DeviceMgr.devicestorage->FreeSpaceInBytes -= filesize;
             DeviceMgr.devicestorage->FreeSpaceInObjects--;
 
+            // AlbumArtist is now sent by our patched libmtp inside the
+            // LIBMTP_Update_Track_Metadata batch, so no post-hoc single-property
+            // call is needed here.
+
             // Only update Album data if transfer was successful.
             if (trackfile->album != NULL) {
                 albumAddTrackToAlbum(albuminfo, trackfile);
@@ -713,6 +726,7 @@
         }
         LIBMTP_destroy_track_t(trackfile);
         LIBMTP_destroy_album_t(albuminfo);
+        g_free(albumArtistTag);
     } else {
         // Generic file upload.
         LIBMTP_file_t *genfile = LIBMTP_new_file_t();
